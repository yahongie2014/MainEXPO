import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 40,
  },
  header: {
    fontSize: 18,
    fontWeight: "100",
    textAlign: "left",
    marginStart: 16,
    marginBottom: 8,
  },
});
export default styles;
